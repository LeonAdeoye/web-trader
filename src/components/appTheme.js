import { createTheme } from '@mui/material/styles';

const AppTheme = createTheme({
    palette: {
        primary:  {
            main: '#404040',
        },
        secondary: {
            main: '#404040',
        },
        action: {
            hover: '#cebcbc',
        },
    }
});

export default AppTheme;
